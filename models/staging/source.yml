version: 2
 
sources:
    - name: jaffle_shop
      description: This is a replica of the Postgres database used by our app
      database: raw
      schema: jaffle_shop
      tables:
          - name: customers
            description: custeomer details
          - name: orders
            description: order details

    - name: stripe
      description: schema from snowflake
      database: raw
      schema: stripe
      tables:
          - name: payment
            description: payment details